# 🔍 Как проверить работу Telegram

## Способ 1: Через браузер (самый простой)

1. Убедитесь, что сервер запущен:
```bash
cd backend
source venv/bin/activate
python3 run.py
```

2. Откройте в браузере:
   - **Тестовый эндпоинт**: http://localhost:8000/api/v1/telegram/test
   - **Проверка конфигурации**: http://localhost:8000/api/v1/telegram/config

3. Вы увидите JSON ответ с информацией о настройках и результатом отправки

## Способ 2: Через curl в терминале

```bash
# Проверка конфигурации
curl http://localhost:8000/api/v1/telegram/config

# Тестовая отправка
curl http://localhost:8000/api/v1/telegram/test
```

## Способ 3: Через Swagger UI (интерактивно)

1. Откройте: http://localhost:8000/docs
2. Найдите раздел "telegram"
3. Нажмите на `/api/v1/telegram/test` → "Try it out" → "Execute"

## 📋 Где смотреть логи сервера

### Если сервер запущен в терминале:
Логи выводятся прямо в терминал, где запущен сервер. Ищите строки с:
- `Attempting to send Telegram message`
- `Telegram API response status`
- `Failed to send Telegram message` (если есть ошибка)

### Пример логов при успешной отправке:
```
INFO - Attempting to send Telegram message. Enabled: True
INFO - Sending message to Telegram. URL: https://api.telegram.org/bot***/sendMessage, Chat ID: 123456789
INFO - Telegram API response status: 200
INFO - Telegram message sent successfully
```

### Пример логов при ошибке:
```
ERROR - TELEGRAM_BOT_TOKEN not configured in .env file
```
или
```
ERROR - Telegram API error: {'ok': False, 'description': 'Bad Request: chat not found'}
```

## 🔧 Проверка .env файла

1. Убедитесь, что файл `.env` находится в папке `backend/`:
```bash
cd backend
ls -la .env
```

2. Проверьте содержимое (НЕ показывайте токен публично!):
```bash
cat .env | grep TELEGRAM
```

Должно быть примерно так:
```
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
TELEGRAM_ENABLED=true
```

3. После изменения .env файла **перезапустите сервер**!

## 🐛 Частые проблемы

### Проблема: "Telegram notifications are disabled"
**Решение**: Убедитесь, что в `.env` есть `TELEGRAM_ENABLED=true`

### Проблема: "TELEGRAM_BOT_TOKEN not configured"
**Решение**: Проверьте, что токен указан в `.env` файле

### Проблема: "chat not found"
**Решение**: 
- Убедитесь, что Chat ID правильный (число, не username)
- Начните диалог с ботом (отправьте `/start`)

### Проблема: "Unauthorized"
**Решение**: Проверьте правильность токена бота

## 📱 Проверка в Telegram

1. Найдите вашего бота в Telegram (по username, который вы указали при создании)
2. Отправьте боту команду `/start`
3. Теперь бот может отправлять вам сообщения

## ✅ Пошаговая проверка

1. ✅ Сервер запущен и работает
2. ✅ Файл `.env` существует в папке `backend/`
3. ✅ В `.env` указаны все три переменные (TOKEN, CHAT_ID, ENABLED=true)
4. ✅ Вы начали диалог с ботом в Telegram
5. ✅ Откройте http://localhost:8000/api/v1/telegram/test
6. ✅ Проверьте ответ - должно быть `"status": "success"`
7. ✅ Проверьте Telegram - должно прийти тестовое сообщение

